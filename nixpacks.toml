[phases.setup]
nixPkgs = ["python310", "nodejs_20", "pnpm"]

[phases.install]
cmds = [
  "pip install uv",
  "cd server && uv sync",
]

[start]
cmd = "uvicorn server.app.main:app --host 0.0.0.0 --port $PORT"
