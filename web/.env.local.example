# Point the frontend to your API (defaults to /api via Vite proxy if unset)
# VITE_API_BASE=http://localhost:8000/api

# Optional: (previously) Jawg token not used now; using CARTO Dark Matter tiles by default.
